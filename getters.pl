% GETTERS

getId(SpaceVar, Id):-
	arg(SpaceVar(1, SpaceVar, Id).

getH(SpaceVar, VarH):-
	arg(SpaceVar,2,VarH).

geth(SpaceVar, Varh):-
	arg(SpaceVar, 3, Varh).

getV(SpaceVar, V):-
	arg(SpaceVar, 4, V).

getv(SpaceVar, Varv):-
	arg(SpaceVar, 5, Varv).

getSurf(SpaceVar, Surf):-
	arg(SpaceVar, 6, Surf).

getCoordinates(SpaceVar, X):-
	arg(SpaceVar, 7, X).

getName(SpaceVar, X):-
	arg(SpaceVar, 8).